import dressDB from "../dress-db.yml";
import skillData from "./skill-data.yml";

export function simulateSubDress({
  includedDresses,
  maxLvDresses,
  mainDress,
  focusOn
}) {
  console.log(includedDresses, maxLvDresses, mainDress, focusOn);
}
